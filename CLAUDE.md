# CLAUDE.md

## 目標管理（だんごカンパニー）
目標・事業・秘書・Ωなど、目標管理の仕事をするときだけ `company/rules.md` を読み、そこに書かれた手順に従う。

## 保存の仕方（2026-09-26 としやす決定）
チャットのセッションでも、作業が終わったら、そのチャットの最後に1回、最新の `main` を取り込んで（`git fetch origin main` → 最新の `main` の上に自分の変更を載せる）から、`main` に直接 push する。作業用のブランチに置いたままにしない。
- なぜ：別のセッションが同時に `main` へ入れると、作業用のブランチが古い `main` のままズレる。定期実行の朝の報告は `main` を読むので、`main` に入っていない変更は届かない
